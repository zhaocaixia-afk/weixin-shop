<view class="nav">
  <block wx:for="{{list}}" wx:key="{{index}}">
    <view class="nav-item {{currentIndex === index ? 'active' : ''}}" bind:tap="handleItemClick" data-index="{{index}}">
      <text>{{item}}</text>
    </view>
  </block>
</view>

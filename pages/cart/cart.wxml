<view class="cart">
  <!-- <scroll-view class="cart-list" scroll-y>
    <block wx:for="{{cartList}}" wx:key="index">
      <cart-list-item goods="{{item}}"/>
    </block>
  </scroll-view>
  <bottom-bar/> -->
</view>
